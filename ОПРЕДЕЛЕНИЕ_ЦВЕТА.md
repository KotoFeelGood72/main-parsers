# Определение цвета автомобиля по фотографии

## 🚀 Быстрый старт

Самый простой способ - использовать встроенный анализ изображений (полностью бесплатно, без API ключей):

```bash
npm install sharp
```

Готово! При парсинге система автоматически определит цвет автомобиля по фотографии, если он не указан в объявлении.

## 📋 Как это работает

1. При парсинге каждого объявления система проверяет, указан ли цвет
2. Если цвет не указан или равен "Неизвестно", система анализирует фотографии автомобиля
3. Определенный цвет автоматически сохраняется в базу данных в поле `exterior_color`

## 🎨 Поддерживаемые цвета

- Белый
- Черный  
- Серебристый
- Серый
- Красный
- Синий
- Зеленый
- Желтый
- Оранжевый
- Коричневый
- Бежевый
- Золотой
- Бронзовый
- Фиолетовый
- Розовый

## ⚙️ Альтернативные варианты

Если нужна более высокая точность, можно использовать AI сервисы (бесплатные лимиты):

### Hugging Face (1000 запросов/месяц бесплатно)
1. Зарегистрируйтесь на https://huggingface.co
2. Создайте API токен
3. Добавьте в `.env`:
   ```
   COLOR_DETECTION_PROVIDER=huggingface
   HUGGINGFACE_API_KEY=ваш_токен
   ```

### Google Vision API (1000 запросов/месяц бесплатно)
1. Создайте проект в Google Cloud
2. Включите Vision API
3. Создайте API ключ
4. Добавьте в `.env`:
   ```
   COLOR_DETECTION_PROVIDER=google
   GOOGLE_VISION_API_KEY=ваш_ключ
   ```

## 📝 Примечания

- Определение цвета происходит автоматически, никаких дополнительных действий не требуется
- Если определение не удалось, в базе сохраняется "Неизвестно"
- Система использует главное изображение или первое доступное фото
- При использовании Sharp (простой метод) нет лимитов на количество запросов

## 🔧 Технические детали

Сервис находится в `src/services/ColorDetectionService.js` и автоматически вызывается при сохранении данных в `src/utils/saveData.js`.

Подробная документация: `COLOR_DETECTION_SETUP.md`
